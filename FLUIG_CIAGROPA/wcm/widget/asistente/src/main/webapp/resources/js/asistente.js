var QlikChatWidget = SuperWidget.extend({
    
    init: function() {
        // Este código se ejecuta cuando el widget se carga en la página
        console.log("Widget de Asistente de Chat Qlik inicializado.");
    }

});

